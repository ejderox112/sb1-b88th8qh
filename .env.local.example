# Copy this to `.env.local` and fill values. Do NOT commit secrets to source control.
# Public client IDs are okay to include (they're not secrets); client secrets must be kept private.

# Google OAuth
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=1033668982412-rper29qh08sc2gievscljuiikpmhuc4k.apps.googleusercontent.com
# If you also have native Android/iOS client IDs, set them here in your local `.env.local`.
# DO NOT commit `.env.local` to version control. The repository already ignores `.env*` files.
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=1033668982412-rper29qh08sc2gievscljuiikpmhuc4k.apps.googleusercontent.com
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=

# Supabase (example: values are already in app.json but you can override locally)
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Notes:
# - For native Android Google Sign-In, create an OAuth client for Android with the correct
#   package name and SHA-1 fingerprint and set EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID.
# - Client secrets (if any) must NOT be stored in the repo. Use OS-level env vars or a secret manager.
# - After changing env values, restart Metro (`npx expo start -c`) and rebuild native apps when necessary.

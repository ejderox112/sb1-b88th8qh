export enum UserRole {
  Admin = 'admin',
  Moderator = 'moderator',
  Runner = 'runner',
  User = 'user',
}

export enum SpaceType {
  Hospital = 'hospital',
  School = 'school',
  Mall = 'mall',
  Cafe = 'cafe',
  Other = 'other',
}

export enum TaskStatus {
  Pending = 'pending',
  Completed = 'completed',
  Rejected = 'rejected',
}

export enum ContentType {
  Photo = 'photo',
  Task = 'task',
  Badge = 'badge',
  Space = 'space',
  Comment = 'comment',
}